import type { NextPage } from "next";
import PageLayout from "../layouts/PageLayout";
import Head from "next/head";
import Image from "next/image";

const Home: NextPage = () => {
  return <PageLayout content={<div>hello</div>} />;
};

export default Home;
